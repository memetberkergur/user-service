# 🌐 HTTP'den HTTPS'e yönlendirme (sadece domain için)
# server {
#     listen 80;
#     server_name service.com;
#     return 301 https://$host$request_uri;
# }

# 🚫 IP üzerinden HTTP erişimini engelle
# server {
#     listen 80 default_server;
#     server_name _;
#     return 444;
# }

# 🔒 IP üzerinden HTTPS erişimini engelle
# server {
#     listen 443 ssl default_server;
#     server_name _;
# 
#     ssl_certificate     /etc/ssl/certs/cert.pem;
#     ssl_certificate_key /etc/ssl/private/key.pem;
#     ssl_dhparam         /etc/ssl/certs/dhparam.pem;
# 
#     return 444;
# }

# ✅ Sadece domain eşleştiğinde çalışan HTTPS bloğu
#server {
#    listen 443 ssl;
#    server_name service.com;
#
#    ssl_certificate     /etc/ssl/certs/cert.pem;
#    ssl_certificate_key /etc/ssl/private/key.pem;
#    ssl_dhparam         /etc/ssl/certs/dhparam.pem;
#    ssl_protocols       TLSv1.2 TLSv1.3;
#    ssl_prefer_server_ciphers on;
#
#    # 📦 Statik dosyalar
#    location /static/ {
#        alias /static/;
#        access_log off;
#        expires 30d;
#    }
#
#    # 🔁 Django + Gunicorn (proxy yönlendirmesi)
#    location / {
#        proxy_pass http://notebook-app:8000;
#
#        # 🧠 Proxy güvenlik başlıkları
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
#}

server {
    listen 80;
    server_name _;
    
    location / {
        proxy_pass http://notebook-app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    location /static/ {
        alias /static/;
        access_log off;
        expires 30d;
    }
}